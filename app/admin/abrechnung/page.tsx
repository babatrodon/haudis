import { BereichFolgt } from "@/components/admin/bereich-folgt";

export default function AbrechnungSeite() {
  return (
    <BereichFolgt
      titel="Abrechnung"
      beschreibung="Provisionen und Umsatz pro Zeitraum"
      wann="Kommt in Sprint 5, zusammen mit dem Accounting."
    />
  );
}
